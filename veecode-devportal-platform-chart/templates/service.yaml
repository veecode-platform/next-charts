apiVersion: v1
kind: Service
metadata:
  name: {{ include "veecode-devportal-platform.fullname" . }}
  labels:
    {{- include "veecode-devportal-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "veecode-devportal-platform.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
